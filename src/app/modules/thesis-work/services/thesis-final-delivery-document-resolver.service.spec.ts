/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ThesisFinalDeliveryDocumentResolverService } from './thesis-final-delivery-document-resolver.service';

describe('Service: ThesisFinalDeliveryDocumentResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ThesisFinalDeliveryDocumentResolverService]
    });
  });

  it('should ...', inject([ThesisFinalDeliveryDocumentResolverService], (service: ThesisFinalDeliveryDocumentResolverService) => {
    expect(service).toBeTruthy();
  }));
});
