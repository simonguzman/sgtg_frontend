/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { SustentationDetailsMapperService } from './sustentation-details-mapper.service';

describe('Service: SustentationDetailsMapper', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SustentationDetailsMapperService]
    });
  });

  it('should ...', inject([SustentationDetailsMapperService], (service: SustentationDetailsMapperService) => {
    expect(service).toBeTruthy();
  }));
});
