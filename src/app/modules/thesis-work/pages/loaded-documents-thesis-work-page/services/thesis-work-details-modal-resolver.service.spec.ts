/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ThesisWorkDetailsModalResolverService } from './thesis-work-details-modal-resolver.service';

describe('Service: ThesisWorkDetailsModalResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ThesisWorkDetailsModalResolverService]
    });
  });

  it('should ...', inject([ThesisWorkDetailsModalResolverService], (service: ThesisWorkDetailsModalResolverService) => {
    expect(service).toBeTruthy();
  }));
});
