/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ThesisWorkDetailsMapperService } from './thesis-work-details-mapper.service';

describe('Service: ThesisWorkDetailsMapper', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ThesisWorkDetailsMapperService]
    });
  });

  it('should ...', inject([ThesisWorkDetailsMapperService], (service: ThesisWorkDetailsMapperService) => {
    expect(service).toBeTruthy();
  }));
});
