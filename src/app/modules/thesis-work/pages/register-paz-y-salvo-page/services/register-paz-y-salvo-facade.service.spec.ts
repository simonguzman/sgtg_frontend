/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RegisterPazYSalvoFacadeService } from './register-paz-y-salvo-facade.service';

describe('Service: RegisterPazYSalvoFacade', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RegisterPazYSalvoFacadeService]
    });
  });

  it('should ...', inject([RegisterPazYSalvoFacadeService], (service: RegisterPazYSalvoFacadeService) => {
    expect(service).toBeTruthy();
  }));
});
