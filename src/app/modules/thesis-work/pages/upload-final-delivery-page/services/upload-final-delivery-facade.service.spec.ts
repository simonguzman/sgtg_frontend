/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UploadFinalDeliveryFacadeService } from './upload-final-delivery-facade.service';

describe('Service: UploadFinalDeliveryFacade', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UploadFinalDeliveryFacadeService]
    });
  });

  it('should ...', inject([UploadFinalDeliveryFacadeService], (service: UploadFinalDeliveryFacadeService) => {
    expect(service).toBeTruthy();
  }));
});
