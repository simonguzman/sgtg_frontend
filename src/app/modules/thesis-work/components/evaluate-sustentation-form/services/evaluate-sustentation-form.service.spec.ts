/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EvaluateSustentationFormService } from './evaluate-sustentation-form.service';

describe('Service: EvaluateSustentationForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EvaluateSustentationFormService]
    });
  });

  it('should ...', inject([EvaluateSustentationFormService], (service: EvaluateSustentationFormService) => {
    expect(service).toBeTruthy();
  }));
});
