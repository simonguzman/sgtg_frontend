/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EvaluateAdvanceFormService } from './evaluate-advance-form.service';

describe('Service: EvaluateAdvanceForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EvaluateAdvanceFormService]
    });
  });

  it('should ...', inject([EvaluateAdvanceFormService], (service: EvaluateAdvanceFormService) => {
    expect(service).toBeTruthy();
  }));
});
