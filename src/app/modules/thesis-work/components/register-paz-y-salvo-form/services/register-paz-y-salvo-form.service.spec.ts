/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RegisterPazYSalvoFormService } from './register-paz-y-salvo-form.service';

describe('Service: RegisterPazYSalvoForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RegisterPazYSalvoFormService]
    });
  });

  it('should ...', inject([RegisterPazYSalvoFormService], (service: RegisterPazYSalvoFormService) => {
    expect(service).toBeTruthy();
  }));
});
