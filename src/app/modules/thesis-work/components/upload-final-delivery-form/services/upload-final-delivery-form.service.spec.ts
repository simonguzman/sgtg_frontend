/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UploadFinalDeliveryFormService } from './upload-final-delivery-form.service';

describe('Service: UploadFinalDeliveryForm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UploadFinalDeliveryFormService]
    });
  });

  it('should ...', inject([UploadFinalDeliveryFormService], (service: UploadFinalDeliveryFormService) => {
    expect(service).toBeTruthy();
  }));
});
